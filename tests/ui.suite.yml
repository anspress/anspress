# Codeception Test Suite Configuration

# suite for UI (acceptance) tests.
# perform tests in browser using WPBrowser or WPWebDriver modules.
class_name: UITester
modules:
    enabled:
        - WPWebDriver
        - \Helper\UI
        - \Helper\Acceptance
